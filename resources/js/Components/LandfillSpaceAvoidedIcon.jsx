/**
 * Client-provided raster for “Landfill space avoided” (dashboard, Resource Intelligence, PDF uses the same file from public).
 */
export default function LandfillSpaceAvoidedIcon({ className = '' }) {
    return (
        <img
            src="/images/landfill-space-avoided-icon.png"
            alt=""
            className={className}
            aria-hidden
            decoding="async"
        />
    );
}
